{
  "name": "ai-code-auditor",
  "type": "module",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npm run lint",
    "deploy": "npx genlayer deploy",
    "test": "cd test && python -m pytest -v"
  },
  "devDependencies": {
    "genlayer-js": "^0.18.3"
  }
}
